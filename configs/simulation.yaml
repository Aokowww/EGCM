# Simulation configuration (edit as needed)
seed_base: 100
nrep: 100
# Random intercept experiment (Chapter 6.1.1)
n_subjects_grid: [20, 50, 100, 200]
n_measurements_grid: [3, 5, 10, 20, 50, 100]
within_subject_rho: 0.5
noise_sd: 0.3
b_null: 0.0
b_alt: 0.2
# Functions used for f and g (randomly sampled per replication)
functions: [linear, square, cubic, tanh, negexp]
